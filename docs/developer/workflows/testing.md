# Testing

There is no automated test suite configured in this repo at the moment. Validation is done via:
- `pnpm build` (typecheck + Vite build)
- `pnpm build:lib` (library build)
- manual smoke tests in the `example/` app

If tests are added later, document the commands here.
